# ONLY LONELY Project Instructions

このプロジェクトでは、必要十分な実装を優先し、要求されていない機能や抽象化を追加しない。

## Scope and Simplicity

- ユーザーが明示した要件だけを実装する。将来利用を推測した機能、設定、汎用化は追加しない。
- 新しいDB列、migration、状態、画面、依存関係を増やす前に、既存構造で安全に実現できないか確認する。
- 動作しているコードの分割、整理、命名変更、リファクタリングは、現在の要求に必要な場合だけ行う。
- 小さな変更を大規模な設計変更へ広げない。複雑化が必要なら、実装前に理由と影響範囲をユーザーへ示す。
- 本番未適用の関連変更は、可能な範囲で一つの小さな変更単位として扱う。

## Verification Proportionality

- 作業中は変更箇所に直接関係する最小限のテストを優先する。
- 全E2E、全migration再構築、負荷試験、Worker dry-run、独立完了監査は、原則としてデプロイ候補が固まった時点で一度まとめて行う。
- 投票秘密性、二重投票防止、認証、個人情報削除、DB整合性に触れる変更は、小変更でも該当する安全テストを省略しない。
- 既知の環境障害に対して同じ確認を繰り返さない。安全な代替確認がなければ `unverified` として報告する。
- fallback、静的検査、dry-runを実ブラウザや本番動作の成功として扱わない。

## Reporting and Approval

- 完了報告には、実装した範囲、実行した確認、未検証項目、本番反映の有無を簡潔に記載する。
- 本番デプロイ、本番D1 migration、本番データ変更は、ユーザーの明示的な依頼がある場合だけ行う。
- 追加提案は実装せず、必要性と費用対効果を短く提示するにとどめる。
